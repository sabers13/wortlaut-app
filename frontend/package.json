{
  "name": "flashcard-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/api/client.test.ts src/study/extra-info-preference.test.ts src/utils/candidate-helpers.test.ts",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
